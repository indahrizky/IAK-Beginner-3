<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="indah.com.iakbeginner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/cherries"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/pizza"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".spalshScreen.SplashActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Order" />
        <activity android:name=".About" />
        <activity android:name=".home_view.HomeActivity" />
        <activity android:name=".list_view.ListActivity"></activity>
    </application>

</manifest>